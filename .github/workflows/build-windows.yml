name: Build Windows

on:
  workflow_call:
    inputs:
      app_env:
        description: Application environment (pre or stable)
        required: false
        type: string
        default: pre
      source_ref:
        description: Git ref to build
        required: false
        type: string
        default: ''
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: build-windows-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_VERSION: '3.44.4'
  GO_VERSION: '1.26.4'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-2022
            arch: amd64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    steps:
      - name: Enable Git long paths
        shell: pwsh
        run: git config --global core.longpaths true

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.source_ref || github.ref }}
          submodules: recursive

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: |
            core/go.sum
            core/Clash.Meta/go.sum
            core/Clash.Meta/third_party/smux/go.sum

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            services/helper -> target
            plugins/rust_api/rust -> target

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Test
        run: flutter test --reporter expanded

      - name: Build Windows packages
        shell: pwsh
        env:
          APP_ENV: ${{ inputs.app_env || 'pre' }}
        run: |
          if ($env:APP_ENV -notin @('pre', 'stable')) {
            throw "Unsupported application environment: $env:APP_ENV"
          }
          $arguments = @('setup.dart', 'windows', '-v', '--env', $env:APP_ENV)
          & dart @arguments
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Verify build artifacts
        shell: pwsh
        run: |
          $files = Get-ChildItem dist -File -Recurse -ErrorAction SilentlyContinue
          if (-not $files) { throw 'No Windows artifacts were produced.' }

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: flclash-windows-${{ matrix.arch }}
          path: dist/**
          if-no-files-found: error
          retention-days: 14
